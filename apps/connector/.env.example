# Path to the connector spec the CLI uses when none is passed on the command
# line. Relative paths inside the spec resolve against the spec's own
# directory, not the directory you run from.
CONNECTOR_SPEC=connector.json
