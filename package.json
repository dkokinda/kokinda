{
  "name": "imessage-bot",
  "version": "1.0.0",
  "description": "iMessage bot that replies to each chat with unique, context-aware messages generated by Claude, via a BlueBubbles server.",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
